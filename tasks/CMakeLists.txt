# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(imu_inemoTaskLib)
ADD_LIBRARY(${IMU_INEMO_TASKLIB_NAME} SHARED 
    ${IMU_INEMO_TASKLIB_SOURCES})
add_dependencies(${IMU_INEMO_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${IMU_INEMO_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${IMU_INEMO_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${IMU_INEMO_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${IMU_INEMO_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${IMU_INEMO_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${IMU_INEMO_TASKLIB_HEADERS}
    DESTINATION include/orocos/imu_inemo)

